{
  "private": true,
  "scripts": {
    "build": "eslint src && spago build --purs-args '--censor-lib --strict'",
    "test": "spago test"
  },
  "devDependencies": {
    "eslint": "^7.11.0"
  },
  "dependencies": {
    "string-similarity": "^4.0.4"
  }
}
